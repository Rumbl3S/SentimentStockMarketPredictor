# Local dataset mode does not require third-party news API keys.
# Optional: path to SEC company_tickers.json for build_local_dataset.py (overrides default search).
# COMPANY_TICKERS_JSON=/absolute/path/to/company_tickers.json
